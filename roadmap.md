# Roadmap
- Replace remaining Hindi UI copy with English and refine the live location trace panel.
- Fix indexed `India` property access in `AdminMap.tsx`.
- Keep user location traces in the admin live feed and remove the admin self-trace control.
